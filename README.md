Kotlin + Spring IO & Boot Web App
=================================

Building and running the project:

    ./gradlew assemble && java -jar build/libs/demo-0.0.1-SNAPSHOT.jar

or:

    ./gradlew bootRun

